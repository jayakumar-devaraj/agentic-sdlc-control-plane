# Spec: <title>

**Status:** Draft | Approved | Built
**Number:** NNN

## Problem

What is wrong today, stated as an observation rather than as a missing feature. If a defect
prompted this, name it and how it was found.

## Scope

What this change covers.

**Not in scope.** Explicitly. A spec without this section grows during the build.

## Success criteria

Verifiable, each with the check that proves it — a command and its expected exit code, not
"works correctly".

| # | Criterion | Verified by |
|---|---|---|
| 1 | | |

## Constraints

What this must not break. Name the exact files, not the categories.

## Open questions

Things that must be answered before or during the build. Delete the section only when it is
genuinely empty; an empty list and an omitted list are different claims.
